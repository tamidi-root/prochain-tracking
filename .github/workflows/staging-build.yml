name: staging-build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      build_script: build-staging
      artifact_name: staging-build-tracking-web
